import wollok.game.*
import jugador.*
import niveles.*
import texto.*
import config.*
import mundo.*
program boulder {

	game.title("boulder")
	
	game.height(14)
	game.width(30)
	game.boardGround("cueva.png")

	game.addVisual(pantallaInicio)

	keyboard.space().onPressDo({
		mundo.iniciarJuego()
	})

	game.start()
}
